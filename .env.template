# rig-vault env template · vault references resolved at dev/build time via `rig-vault env-resolve`
# generated: 2026-05-17T02:09:58.366Z
# source: .env.local
#
# Format: KEY=vault:<credential-id>  (resolved via vault canonical · F-008 18-field row)
# To produce a runtime .env from this template:
#   rig-vault env-resolve --from=.env.template --eval | source /dev/stdin
# Or run wrapped:
#   eval $(rig-vault env-resolve --from=.env.template --eval) && <your command>

# vault row loopsmith-hello.NEXT_PUBLIC_SUPABASE_URL (per-project)
NEXT_PUBLIC_SUPABASE_URL=vault:loopsmith-hello-next-public-supabase-url
# vault row loopsmith-hello.NEXT_PUBLIC_SUPABASE_ANON_KEY (per-project)
NEXT_PUBLIC_SUPABASE_ANON_KEY=vault:loopsmith-hello-next-public-supabase-anon-key
